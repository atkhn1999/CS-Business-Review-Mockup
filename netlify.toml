# Root netlify config to avoid submodule checkout issues
[build]
  command = npm run build --prefix workspace/success-review
  publish = workspace/success-review/dist
  environment = { NODE_VERSION = 18 }

[[redirects]]
  from = /*
  to = /index.html
  status = 200

