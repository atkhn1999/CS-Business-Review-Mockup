# Root Netlify config for building the app in workspace/success-review
[build]
  command = "npm run build --prefix workspace/success-review"
  publish = "workspace/success-review/dist"

  [build.environment]
    NODE_VERSION = "18"

[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200

